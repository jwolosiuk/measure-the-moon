// The synthetic Moon renderer lives in js/demo.js so that the app ships it too
// (it powers the demo mode). Tests grade the analyser against the same code
// the browser runs.
export { renderMoon } from '../js/demo.js';
